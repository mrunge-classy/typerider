node_modules/

# Runtime state: leaderboard, deleted rows, settings, archived boards.
data/

# Local working notes and the real deployment runbook (server address,
# credentials location, port map) — deliberately not published.
DEPLOYMENT.md
prompt
clone
api
sales
settings
stories-card
stylelib
ddos

# Admin credentials never belong in this repo — the encrypted copy lives in the
# private typerider-leaderboard repo, the working copy in ~/.config/.
admin.pw
admin.pw.*
*.pw
*.pw.gpg
